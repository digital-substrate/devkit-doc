build/
__pycache__/
.DS_Store
.idea/

# Claude Code workspace — local only, not for distribution
CLAUDE.md
.claude/

# Node API reference (sphinx-js + TypeDoc) — installed via root package.json
node_modules/
