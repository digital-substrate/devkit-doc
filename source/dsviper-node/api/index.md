# dsviper for Node.js API

The complete class reference for the `@digitalsubstrate/dsviper` binding, generated from its TypeScript declarations (`index.d.ts`) by TypeDoc via sphinx-js. Each page opens with a scannable summary table, then the full reference. The class set mirrors the dynamic API of the guide pages.

```{toctree}
:maxdepth: 1

types
values
attachments
commit
database
dsm
blobs
serialization
core
```
